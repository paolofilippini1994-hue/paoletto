function Title({children}) {
  return (
    <h1 style={{ color: "green", fontFamily: "monospace", textAlign: "center"}} > {children}
    </h1>
  );
}

export default Title;


// function viewData() {
    // const data = new Date();
        //return (
        // <h3>{oggi.toLocalString()}</h3>
        //)
//}

// export default viewDate;